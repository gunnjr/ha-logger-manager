{
  "config": {
    "step": {
      "user": {
        "title": "Logger Manager",
        "description": "Set up the Logger Manager integration to manage Home Assistant logger levels."
      }
    },
    "abort": {
      "already_configured": "Logger Manager is already configured. Only one instance is allowed."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Logger Manager Options",
        "description": "Configure Logger Manager options."
      }
    }
  }
}
